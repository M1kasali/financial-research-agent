# 新增研究任务参考草案核查

本批完成的是作者辅助标注材料，不是 B 榜答案、正式 gold、Agent 预测或真实模型实验。

## 产物与验证

- 作者草案：`docs/reference-drafts-v1.json`，12 题逐项结论、证据导航、缺口和建议评分要点。
- 编译工具：`scripts/prepare_reference_drafts.py`，绑定冻结题集版本、文档版本、chunk 哈希和唯一原文定位短语；检查引用在每题文档范围内。
- 本次输出：`experiments/runs/reference-drafts-20260913T174844Z-f9894e41/`，包含 `review_packet.md`、`review_packet.json` 和 `summary.json`。
- 46 处引用绑定（按题计数，非 46 个不同文档），3 项 Decimal 算术核对。短语仅用于定位，完整 chunk 随包保存供人工阅读；匹配成功不代表断言被原文支持。
- 全部 198 项测试通过，其中新增 15 项测试覆盖伪造审核状态、B 回放题集拒绝、越范围引用、错误/重复定位、原文改动、数值子串误匹配等。新增代码 Ruff 检查通过。
- 原 `labels.pending.json` 仍全部为 pending、expected=null；已批准 gold=0，真实模型调用=0。

## 覆盖情况

| 题号 | 草案覆盖 | 必须保留的边界 |
| --- | --- | --- |
| 001、002 | 财务字段及收入同比 | 比亚迪同表比较值；区分单位、合并口径与客户/子公司数值 |
| 003 | 跨公司现金流/净利润比较 | 草案明确采用含少数股东损益的合并净利润；正式题目仍应消除术语歧义 |
| 004、005 | 合同部分条款 | 不同发行人，非默认主合同/补充合同关系；尚未覆盖所有终止/违约约定 |
| 006、007 | 保险部分条款 | 养老与特种车产品隔离；材料分支不等于全部同时满足；等待期零词面命中不等于不存在 |
| 008、009 | 监管关键条文 | 报告期限绑定主体和起算点；不补造保管年限；不宣称核验了外部现行效力 |
| 010 | 两份研究材料的短摘要 | 银保前瞻与安全运营案例分开，案例效果不是本 Agent 效果 |
| 011 | 证据不足答复 | 未获得2028经审计实际结果证据；全文词面检索不是穷尽语义证明 |
| 012 | 公司/母公司口径字段 | 公司利润表不等于合并利润表；归母净利润不等于母公司单体净利润 |

5 题标为 answerable_draft，7 题标为 partial_needs_scope_review。两者都不是人工审核通过。
计算核对：收入同比 3.4568%；两家公司现金流/合并净利润分别为 1.7516 倍、1.1982 倍。
数字仅来自给定比赛语料；未对现实财务真实性作外部核验。计算器不自动证明主体、单位、期间解释正确。

## 复现

```bash
.venv/bin/python scripts/prepare_reference_drafts.py --suite data/local/evaluation-20260913T173440Z-915bf57a/new_tasks/cases.json
.venv/bin/pytest -q
```

题集一旦重写必须生成新版本，不覆盖旧冻结文件。脚本每次输出新的本地目录，不改写标签。生成包含原始文档文本，仅供本地使用，未经授权不要上传公开仓库。

## 接下来

先把 7 道宽泛题拆成明确可覆盖的小任务，尤其是合同终止/费用以及分险种条件；补齐证据后由独立人工审核参考答案及评分要点。不得把本批 assistant 草案自动转成 human_reviewed。

这批题已用于开发分析，应视为开发候选，不因改名就成为独立留出集。后续另建未参与调试的文档/任务留出集，再比较原方案、新固定 RAG 和动态 Agent。真实模型运行前仍需明确模型、数据外发范围与调用预算；没有金标时只报告运行、引用完整性和成本等可观测指标，不报告准确率。
