# 首次私有发布检查

日期：2026-09-16。目标：`M1kasali/financial-research-agent`，保持私有，不修改原团队仓库。

## 范围

- 提交应用源码、保留来源的团队组件、离线测试、实验辅助脚本、技术文档和合成示例。
- 排除 `.env`、密钥文件、原始 PDF、比赛题目/语料、索引、数据库、完整 API 响应和运行目录。
- 排除个人求职材料 `docs/handoff/`、内部待办、人工参考草案及评测配方；原文件保留本地。
- 添加空密钥 `.env.example`，不复制已有密钥。
- 不授予开源许可证；来源与公开前需确认的事项见 `NOTICE.md`。

## 本次改动

仅整理发布内容：精简 README、明确可克隆运行的入口、增加三个合成分块及对应回归测试、强化忽略规则。未修改模型策略、默认参数或迁入算法，也未新增在线模型调用。

## 已执行检查

- 完整502项测试及Ruff检查通过（原有501项＋新增合成示例回归）。
- `prepare` 对两份虚构合同完成检索和压缩：2个候选、2个窗口、238字符、0次模型调用。
- 七种合成问答协议场景全部符合预期；模型为预设响应，网络被禁用。
- 两个合成旧运行时场景完成；不是当前模型自主能力评测。
- 三个本机历史案例回放通过SHA-256检查，0次新增API请求；原记录不上传，克隆后不能直接完整回放。

- 仅导出暂存区文件的干净副本，502项测试及七种离线协议演示通过；已确认导入的是导出副本，而不是原工作目录。
- 从导出副本构建wheel并安装到独立目录，502项测试再次通过；复用已有第三方依赖，不宣称完成全新操作系统安装验证。
- 首次无构建隔离的纯离线打包因现有环境缺少setuptools而失败；按标准构建隔离安装构建依赖后打包成功，未修改现有运行依赖。
- 暂存文件通过常见凭证模式、现有本地密钥精确匹配、禁传路径、二进制及大文件检查；未输出或持久化密钥值。
- 108个迁入源码／测试文件与来源清单SHA-256全部一致。迁入文件有3处原有文件末尾空行告警，为保留来源哈希未修改；其余暂存文件空白检查通过。
- README内相对文件链接均指向此次提交中的文件；历史文档中的本地依赖已另作说明。

远端可见性和提交哈希在推送后独立核对。上述扫描未发现可疑项，不等于绝对安全，也不能替代未来公开前的授权与人工审阅。

## 使用边界

- README中的合成数据和模拟模型入口可离线运行，不代表真实模型准确率。
- 部分历史文档链接指向未发布的本地运行记录、个人交接材料或评测配方；保留为原始阶段记录，不作为快速开始依赖。
- 比赛名次是原团队方案成绩；没有核实同榜有效队伍数，因此不添加“前10%”。
- 迁入代码保留来源记录，不代表全部历史分支接入当前CLI。
