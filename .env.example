# Template only. Never commit .env or a real API key.
# Requests are disabled unless the CLI is explicitly given --execute.
DASHSCOPE_API_KEY=
QWEN_MODEL=qwen3.7-flash
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
