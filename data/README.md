# 本地数据

当前通过 `configs/datasets.example.json` 的明确路径读取旧项目 JSONL，不复制大文件、不修改原数据。配置路径相对项目根目录。

已核对的一套文本：68 份文档、8,925 条普通分块；另有 38,530 条原子分块。B 榜题目 100 道。不能据此认定全部官方材料齐全或语料版本与最高分运行相同。

`data/local/`、`data/indexes/` 预留给本地资源且被 Git 忽略。不要把密钥、未获授权材料或历史预测答案作为新项目输入打包发布。
